FROM php:7.1-fpm

# Debian系のディストリビューションのパッケージ管理ツール
RUN apt-get update

# zip、unzipコマンドをインストール
RUN apt-get install -y zip unzip

# 必要なPHP拡張モジュールをインストール
RUN apt-get install -y libpq-dev \
    && docker-php-ext-install pdo_mysql pdo_pgsql

# ワーキングディレクトリを指定
WORKDIR /var/www/html